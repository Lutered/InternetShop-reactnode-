import {$authHost, $host} from "../http";

const productUrl = 'api/products';

export const login = async (email, password) => {
   // const {data} = await $host.post(`${productUrl}/create`, product);
    //return data;
};